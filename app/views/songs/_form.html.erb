<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.label :artist %>
  <%= f.collection_select :artist_id, @artists, :id, :name, include_blank: true %>

  <%= f.submit %>
<% end %>