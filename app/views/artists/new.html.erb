<h1>Create an Artist</h1>

<%= form_for @artist  do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.collection_select(:id, @songs, :id, :title) %><br>

  <br />
  <%= f.submit %>
<% end %>
